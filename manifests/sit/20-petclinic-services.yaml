---
apiVersion: v1
kind: Service
metadata: { name: customers-service, namespace: sit }
spec:
  ports: [{ port: 8081, targetPort: 8081 }]
  selector: { app: customers-service }
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: customers-service, namespace: sit, labels: { app: customers-service } }
spec:
  replicas: 1
  selector: { matchLabels: { app: customers-service } }
  template:
    metadata: { labels: { app: customers-service } }
    spec:
      containers:
      - name: app
        image: localhost:5000/petclinic-customers-service:v2.2
        imagePullPolicy: Always
        ports: [{ containerPort: 8081 }]
        envFrom:
        - { configMapRef: { name: petclinic-db-config } }
        - { secretRef:    { name: petclinic-db-credentials } }
        readinessProbe:
          httpGet: { path: /actuator/health, port: 8081 }
          initialDelaySeconds: 30
          periodSeconds: 10
          failureThreshold: 30
        resources:
          requests: { cpu: 100m, memory: 384Mi }
          limits:   { cpu: 1,    memory: 768Mi }
---
apiVersion: v1
kind: Service
metadata: { name: vets-service, namespace: sit }
spec:
  ports: [{ port: 8083, targetPort: 8083 }]
  selector: { app: vets-service }
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: vets-service, namespace: sit, labels: { app: vets-service } }
spec:
  replicas: 1
  selector: { matchLabels: { app: vets-service } }
  template:
    metadata: { labels: { app: vets-service } }
    spec:
      containers:
      - name: app
        image: localhost:5000/petclinic-vets-service:v2.2
        imagePullPolicy: Always
        ports: [{ containerPort: 8083 }]
        envFrom:
        - { configMapRef: { name: petclinic-db-config } }
        - { secretRef:    { name: petclinic-db-credentials } }
        readinessProbe:
          httpGet: { path: /actuator/health, port: 8083 }
          initialDelaySeconds: 30
          periodSeconds: 10
          failureThreshold: 30
        resources:
          requests: { cpu: 100m, memory: 384Mi }
          limits:   { cpu: 1,    memory: 768Mi }
---
apiVersion: v1
kind: Service
metadata: { name: visits-service, namespace: sit }
spec:
  ports: [{ port: 8082, targetPort: 8082 }]
  selector: { app: visits-service }
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: visits-service, namespace: sit, labels: { app: visits-service } }
spec:
  replicas: 1
  selector: { matchLabels: { app: visits-service } }
  template:
    metadata: { labels: { app: visits-service } }
    spec:
      containers:
      - name: app
        image: localhost:5000/petclinic-visits-service:v2.2
        imagePullPolicy: Always
        ports: [{ containerPort: 8082 }]
        envFrom:
        - { configMapRef: { name: petclinic-db-config } }
        - { secretRef:    { name: petclinic-db-credentials } }
        readinessProbe:
          httpGet: { path: /actuator/health, port: 8082 }
          initialDelaySeconds: 30
          periodSeconds: 10
          failureThreshold: 30
        resources:
          requests: { cpu: 100m, memory: 384Mi }
          limits:   { cpu: 1,    memory: 768Mi }
---
apiVersion: v1
kind: Service
metadata: { name: api-gateway, namespace: sit }
spec:
  ports: [{ port: 8080, targetPort: 8080 }]
  selector: { app: api-gateway }
---
apiVersion: apps/v1
kind: Deployment
metadata: { name: api-gateway, namespace: sit, labels: { app: api-gateway } }
spec:
  replicas: 1
  selector: { matchLabels: { app: api-gateway } }
  template:
    metadata: { labels: { app: api-gateway } }
    spec:
      containers:
      - name: app
        image: localhost:5000/petclinic-api-gateway:v2.2
        imagePullPolicy: Always
        ports: [{ containerPort: 8080 }]
        envFrom:
        - { configMapRef: { name: petclinic-db-config } }
        readinessProbe:
          httpGet: { path: /actuator/health, port: 8080 }
          initialDelaySeconds: 30
          periodSeconds: 10
          failureThreshold: 30
        resources:
          requests: { cpu: 100m, memory: 384Mi }
          limits:   { cpu: 1,    memory: 768Mi }
